{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "base.db",
            "database": "${workspaceFolder:cafeteria_menu}/base.db"
        }
    ]
}